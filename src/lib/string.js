// STRING METHODS //////////////////////////////////////////////////////////////
let string
export default string = {
  // Tests that the module was imported correctly
  test: () => 'Hello from string module',
}
